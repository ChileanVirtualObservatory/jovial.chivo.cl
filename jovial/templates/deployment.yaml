apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: {{ template "jovial.fullname" . }}
  labels:
    app: {{ template "jovial.name" . }}
    chart: {{ template "jovial.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.jovial.kubernetes.deployment.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "jovial.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "jovial.name" . }}
        release: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ template "jovial.fullname" . }}
      automountServiceAccountToken: true
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.jovial.kubernetes.deployment.image.repository }}:{{ .Values.jovial.kubernetes.deployment.image.tag }}"
          imagePullPolicy: {{ .Values.jovial.kubernetes.deployment.image.pullPolicy }}
          command:
{{ toYaml .Values.jovial.kubernetes.deployment.command | indent 12 }}
          ports:
            - name: frontend
              containerPort: {{ .Values.jovial.kubernetes.deployment.ports.frontend }}
              protocol: TCP
            - name: proxy-api
              containerPort: {{ .Values.jovial.kubernetes.deployment.ports.proxy }}
              protocol: TCP
            - name: rest-api
              containerPort: {{ .Values.jovial.kubernetes.deployment.ports.rest }}
              protocol: TCP
          volumeMounts:
            - name: jovial-configmap
              mountPath: /config/
          livenessProbe:
            tcpSocket:
              port: frontend
          readinessProbe:
            tcpSocket:
              port: frontend
          resources:
{{ toYaml .Values.jovial.kubernetes.deployment.resources | indent 12 }}
      volumes:
        - name: jovial-configmap
          configmap:
            name: {{ template "jovial.fullname" . }}-config
    {{- with .Values.jovial.kubernetes.deployment.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.jovial.kubernetes.deployment.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.jovial.kubernetes.deployment.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
